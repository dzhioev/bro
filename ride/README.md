# bro-ride

`bro-ride` provides the `ride` managed-workspace runtime and its Claude Code harness implementation. It depends on the `bro` framework for workspace, launch, and bro composition primitives.
