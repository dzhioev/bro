# bro-ride

`bro-ride` provides the `ride` managed-workspace runtime — workspace creation, container execution, and scoped credential hydration — with both harness adapters: the Claude Code harness and the native adapter, which spawns the `bro` command that `bro-native` installs. It depends on the `bro` framework for configuration, launch, and bro composition primitives.
