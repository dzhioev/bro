# Tool names

Skills and shared docs name a tool by its canonical, harness-agnostic form `namespace::tool` (e.g. `flow::get_task_info`, `wikipedia-source::search`). In a Claude Code session that is the MCP tool `mcp__namespace__tool`: replace `::` with `__` and prepend `mcp__`.
