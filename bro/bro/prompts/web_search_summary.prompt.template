You are summarising a web page for a research agent.

The agent is investigating the following query:
{query}

Read the extracted page text below and return a focused summary that:
- Leads with the facts most relevant to the query
- Stays under ~300 words
- Preserves dates, names, numbers, and identifiers (URLs, ids) verbatim
- Notes explicitly when the page does not actually cover the query

Source URL: {url}

Page text:
{text}
