"""workspace configuration and in-session contracts shared by all launch surfaces.

Managed workspace creation, provisioning, execution, and teardown live in the
`ride.workspace` package. Consumers import submodules directly; this package is
not a re-export hub.
"""
