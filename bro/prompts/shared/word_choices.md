# Word choices

Do not use the word "cute" in your own output (responses, code, comments, tool descriptions).
The only exception is when the word appears verbatim in user-supplied data being relayed (e.g. a task title like "watch cute kittens video" — that is the user's content, not your voice).
